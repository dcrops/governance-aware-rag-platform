services:

  crc-rag-app:
    build: .

    container_name: crc-rag-mvp

    ports:
      - "8501:8501"

    env_file:
      - .env

    volumes:
      - ./data:/app/data
      - ./logs:/app/logs

    restart: unless-stopped